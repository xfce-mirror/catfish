include:
  - project: 'xfce/xfce4-dev-tools'
    file: '/ci/build_project.yml'

build-gcc-meson:
  rules:
    - when: never

build-clang-meson:
  rules:
    - when: never

build-meson:
  extends: .build-meson
