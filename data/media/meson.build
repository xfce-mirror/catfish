sizes = ['16x16', '48x48', '128x128', 'scalable']

foreach size: sizes
  install_subdir(
    size,
    install_dir: datadir / 'icons' / 'hicolor' / size / 'apps',
    strip_directory: true,
  )
endforeach
